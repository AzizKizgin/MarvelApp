import {API_URL} from '../utils/consts';

const urls = {
  getCharacters: `${API_URL}/characters`,
  getComics: `${API_URL}/comics`,
  getCreators: `${API_URL}/creators`,
  getEvents: `${API_URL}/events`,
  getSeries: `${API_URL}/series`,
  getStories: `${API_URL}/stories`,
};

export default urls;
